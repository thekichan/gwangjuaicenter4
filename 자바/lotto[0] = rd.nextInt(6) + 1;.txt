lotto[0] = rd.nextInt(6) + 1;
		System.out.println(lotto[0]);
		rd.nextInt(10); // 0  ~  9
		
		// 이전에 뽑았던 값들을 탐색하면서 만약, 중복이라면
		// 다시 뽑는다 !!
		lotto[1] = rd.nextInt(45) + 1;
		// 이와 같은 방식으로 로또 번호 6개를 뽑아 lotto배열에 대입!!
		// 단, 중복이 발생하면 안된다